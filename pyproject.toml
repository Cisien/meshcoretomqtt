[project]
name = "meshcoretomqtt"
version = "1.3.0.0-preview"
requires-python = ">=3.11"
dependencies = [
    "ed25519_orlp",
    "paho-mqtt",
    "tomli",
    "pyserial",
]

[project.optional-dependencies]
test = ["pytest>=7.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
