[project]
name = "meshcoretomqtt"
version = "0.1.0"
requires-python = ">=3.11"
version = "0.1.0"
dependencies = [
    "paho-mqtt",
    "tomli",
    "pyserial",
]

[project.optional-dependencies]
test = ["pytest>=7.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
